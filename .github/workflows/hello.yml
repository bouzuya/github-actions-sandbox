name: Hello, World!
on: push
jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
      - uses: actions/checkout@v2
      - uses: ./.github/actions/action-a
